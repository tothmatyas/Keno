<Window x:Class="KenoGui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KenoGui"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Felső gombok -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Szelvények betöltése"
                    Width="170"
                    Click="BtnBetoltes_Click"
                    Margin="0,0,10,0"/>
            <Button Content="Napi sorsolás"
                    Width="120"
                    Click="BtnSorsolas_Click"/>
        </StackPanel>

        <!-- Fő tartalom -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <!-- Szelvény lista -->
            <ListBox x:Name="lbSzelvenyek"
                     Grid.Column="0"
                     SelectionChanged="lbSzelvenyek_SelectionChanged"
                     Margin="0,0,10,0"/>

            <!-- Sorsolt számok -->
            <ListBox x:Name="lbSorsolt"
                     Grid.Column="1"
                     Width="70"
                     Margin="0,0,10,0"/>

            <!-- Számrács -->
            <Grid x:Name="gridSzamok"
                  Grid.Column="2"
                  Background="LightGreen"/>
        </Grid>

        <!-- Alsó rész -->
        <Grid Grid.Row="2" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="txtSzorzó"
                       FontSize="16"
                       VerticalAlignment="Center"/>

            <TextBlock x:Name="txtNyeremeny"
                       Grid.Column="1"
                       FontSize="20"
                       Foreground="Red"
                       FontWeight="Bold"
                       VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</Window>
